---
name: pytest

on:
  push:
    branches: [main]
  pull_request:

env:
  FORCE_COLOR: 1

jobs:

  test-ace:
    name: ACE (${{ matrix.python-version }}, ${{ matrix.os }})
    runs-on: ${{ matrix.os }}

    strategy:
      fail-fast: true
      matrix:
        os: ["ubuntu-22.04"]
        python-version: ["3.10"]

    defaults:
      run:
        shell: bash -l {0}

    steps:
    - uses: actions/checkout@v6

    - uses: julia-actions/setup-julia@v2
      with:
        version: '1.6'

    - uses: mamba-org/setup-micromamba@v3
      with:
        environment-file: environment_ace.yml
        environment-name: gha-test-env
        cache-environment: true
        create-args: >-
          python=${{ matrix.python-version }}

    - name: ACE install
      run: ./install_ace.sh

    - name: Test ACE
      run: pytest --cov=mlptrain -k "not test_openmm"

    - name: Upload coverage reports to Codecov
      uses: codecov/codecov-action@v6
      with:
          flags: python-${{ matrix.python-version }}-ace
          token: ${{ secrets.CODECOV_TOKEN }}
          slug: duartegroup/mlp-train

  test-mace:
    name: MACE (${{ matrix.python-version }}, ${{ matrix.os }})
    runs-on: ${{ matrix.os }}

    # The pixi lock pins CUDA builds (system-requirements cuda = "12"); this lets
    # them install on the GPU-less runner. They run fine on CPU for the tests.
    env:
      CONDA_OVERRIDE_CUDA: "12.0"

    strategy:
      fail-fast: true
      matrix:
        os: ["ubuntu-22.04"]
        python-version: ["3.10"]

    defaults:
      run:
        shell: bash -l {0}

    steps:
    - uses: actions/checkout@v6

    - uses: prefix-dev/setup-pixi@v0.8.10
      with:
        environments: mace
        cache: true

    - name: Test MACE install
      run: pixi run -e mace test

    - name: Upload coverage reports to Codecov
      uses: codecov/codecov-action@v6
      with:
          flags: python-${{ matrix.python-version }}-mace
          token: ${{ secrets.CODECOV_TOKEN }}
          slug: duartegroup/mlp-train
